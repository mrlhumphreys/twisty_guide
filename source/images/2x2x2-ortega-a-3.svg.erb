<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN"
  "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg xmlns="http://www.w3.org/2000/svg" version="1.1" width="180" height="180">
  <%
    up_squares = [
      [ 
        nil,
        nil
      ],
      [
        nil,
        {colour: data.colours.white, opacity: data.opacity.full }
      ]
    ]
  %>

  <%= partial('grid', locals: {grid: top_grid(x: 30, y: 30, units: 30, squares: up_squares)} ) %>

  <%
    front_squares = [
      [ 
        nil,
        nil
      ],
      [
        nil,
        nil
      ]
    ]
  %>

  <%= partial('grid', locals: {grid: front_grid(x: 30, y: 60, units: 30, squares: front_squares)} ) %>

  <%
    right_squares = [
      [ 
        nil,
        nil
      ],
      [ 
        nil,
        nil
      ]
    ]
  %>

  <%= partial('grid', locals: {grid: right_grid(x: 90, y: 60, units: 30, squares: right_squares)} ) %>
</svg>

